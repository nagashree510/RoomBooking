package apis;

import SearchManager.BuildingSearcher;
import SearchManager.Searcher;
import data.Building;
import data.Floor;
import data.Slot;

public class SearchRoomAPI {
    public Room searchRoom(Building building, Floor floor,Slot slot){
        Searcher searcher;
        List<Rooms> list= new BuildingFilter().filter;
        List<Room> list2=new

    }
}
